import turtle

window = turtle.Screen()
window.setup(height=600,width=700)
pen = turtle.Turtle()

for i in range(0,4):
# Draw vertical line
 pen.left(120)  # Turn left to face upward
 pen.forward(110)


turtle.done() # not exit the box